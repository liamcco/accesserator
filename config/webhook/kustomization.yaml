apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- manifests.yaml
- webhook-certificate.yaml
- service.yaml

transformers:
- webhook-nameprefix-transformer.yaml

patches:
- path: webhook-patch.yaml